let express = require('express');
let app = express();
console.log("Hello World")
absolutePath = __dirname + relativePath/index.html;
app.get('/', (req, res) => {
    res.sendFile(absolutePath)
  })

























 module.exports = app;
